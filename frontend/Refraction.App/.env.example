# Vite reads .env files automatically in local development.
# Local default:
VITE_API_BASE_URL=http://localhost:5057

# Public deployment:
# - leave VITE_API_BASE_URL unset to use the same public origin and a reverse-proxied /api path, or
# - set it to a full public API base URL such as https://refraction.example.com
